import { FC } from 'react';

import { ProfileContent } from '@components/Profile/ProfileContent';

const ProfileContentPage: FC = () => {
  return <ProfileContent />;
};

export default ProfileContentPage;
